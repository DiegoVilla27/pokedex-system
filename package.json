{
  "name": "@pokedex/source",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "npx nx run-many -t dev",
    "build": "npx nx run-many -t build",
    "test": "npx nx run-many -t test",
    "graph": "npx nx graph",
    "docker": "docker compose up build -d",
    "clean": "rm -rf node_modules .nx pnpm-lock.yaml && pnpm i"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "nx": "23.1.2",
    "prettier": "^3.8.1",
    "typescript": "~6.0.3"
  }
}